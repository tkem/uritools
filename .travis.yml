language: python
python:
  - 2.7
  - 3.2
  - 3.3
  - 3.4
install:
  - pip install .
  - pip install coverage coveralls
script:
  - nosetests --with-coverage --cover-package=uritools
after_success:
  - coveralls
